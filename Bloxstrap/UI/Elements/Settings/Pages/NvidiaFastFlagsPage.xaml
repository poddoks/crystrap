<ui:UiPage
    x:Class="Bloxstrap.UI.Elements.Settings.Pages.NvidiaFastFlagsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Bloxstrap.UI.Elements.Controls"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="NVIDIA FastFlags"
    Scrollable="True">

    <StackPanel Margin="0,0,14,14">
        <TextBlock
            FontSize="14"
            Margin="0,0,0,8"
            TextWrapping="Wrap"
            Text="Configure NVIDIA driver FastFlags using a simplified NIP-based editor. Crystrap will download nvidiaProfileInspector automatically when you apply changes." />

        <ui:CardAction Click="OpenRawEditor_Click" Margin="0,8,0,0" Icon="Edit24">
            <StackPanel>
                <TextBlock FontSize="14" Text="Advanced Editor" />
                <TextBlock Margin="0,2,0,0" Padding="0,0,16,0" FontSize="12" Text="Open the raw NVIDIA Profile Inspector editor for direct setting control." />
            </StackPanel>
        </ui:CardAction>

        <ui:CardAction Click="OpenFastFlagSettings_Click" Margin="0,8,0,0" Icon="Flag24">
            <StackPanel>
                <TextBlock FontSize="14" Text="FastFlag Settings" />
                <TextBlock Margin="0,2,0,0" Padding="0,0,16,0" FontSize="12" Text="Jump back to Crystrap's regular FastFlags page." />
            </StackPanel>
        </ui:CardAction>

        <controls:OptionControl Header="Apply Changes" Description="Apply only the modified NVIDIA FastFlags to the driver.">
            <ui:Button Content="Apply" Icon="CheckmarkCircle24" Click="Apply_Click"/>
        </controls:OptionControl>

        <TextBlock Margin="0,16,0,0" FontSize="17.65" FontWeight="Medium" Text="FastFlags" />

        <ui:CardExpander Margin="0,8,0,0" IsExpanded="False">
            <ui:CardExpander.Header>
                <StackPanel>
                    <TextBlock FontSize="14" Text="Game Details" />
                    <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Text="Image quality, frame pacing, and performance-related options." />
                </StackPanel>
            </ui:CardExpander.Header>

            <StackPanel>
                <controls:OptionControl Header="Texture Quality (LOD Bias)" Description="Lower values produce sharper textures. Higher values improve performance at the cost of detail.">
                    <StackPanel Width="220">
                        <Slider Minimum="0" Maximum="120" TickFrequency="5" IsSnapToTickEnabled="True" Value="{Binding TextureLodBias, Mode=TwoWay}" />
                        <TextBlock Margin="0,4,0,0" FontSize="12" Foreground="Gray" Text="{Binding TextureLodBiasLabel}" />
                    </StackPanel>
                </controls:OptionControl>

                <controls:OptionControl Header="MFAA" Description="Improves anti-aliasing quality by combining MSAA samples across frames with minimal performance impact.">
                    <ui:ToggleSwitch IsChecked="{Binding EnableMFAA}" />
                </controls:OptionControl>

                <controls:OptionControl Header="FXAA" Description="Smooths jagged edges across the entire frame with minimal performance impact.">
                    <ui:ToggleSwitch IsChecked="{Binding EnableFXAA}" />
                </controls:OptionControl>

                <controls:OptionControl Header="Gamma Correction" Description="Adjusts how brightness values are mapped, affecting contrast and shadow detail.">
                    <ui:ToggleSwitch IsChecked="{Binding EnableGamma}" />
                </controls:OptionControl>

                <controls:OptionControl Header="DLSS Super Resolution" Description="Enables the NVIDIA DLSS Super Resolution override flag for supported GPUs and driver paths.">
                    <ui:ToggleSwitch IsChecked="{Binding EnableDlssSuperResolution}" />
                </controls:OptionControl>

                <controls:OptionControl Header="DLSS Frame Generation" Description="Enables the NVIDIA DLSS Frame Generation override flag for supported GPUs and driver paths.">
                    <ui:ToggleSwitch IsChecked="{Binding EnableDlssFrameGen}" />
                </controls:OptionControl>

                <controls:OptionControl Header="Benchmark Overlay" Description="Displays NVIDIA driver-level performance overlays such as FPS graphs and present timing data.">
                    <ComboBox Width="220" ItemsSource="{Binding BenchMarkOverlayModes}" SelectedItem="{Binding BenchMarkOverlayMode}" />
                </controls:OptionControl>

                <controls:OptionControl Header="Frame Rate Limiter" Description="Limits the maximum frame rate using the NVIDIA driver.">
                    <TextBox Width="220" Text="{Binding FrameRateLimit, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                </controls:OptionControl>

                <controls:OptionControl Header="Background Application Frame Rate Limit" Description="Limits the frame rate when Roblox is in the background to reduce GPU and CPU usage.">
                    <TextBox Width="220" Text="{Binding BackgroundFrameRateLimit, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                </controls:OptionControl>
            </StackPanel>
        </ui:CardExpander>

        <ui:CardExpander Margin="0,8,0,0">
            <ui:CardExpander.Header>
                <StackPanel>
                    <TextBlock FontSize="14" Text="Low Latency Modes" />
                    <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Text="Controls how many frames are queued to reduce input latency." />
                </StackPanel>
            </ui:CardExpander.Header>

            <StackPanel>
                <controls:OptionControl Header="CPL Low Latency Mode" Description="Limits CPU-rendered frames queued by the NVIDIA Control Panel.">
                    <ComboBox Width="220" ItemsSource="{Binding CplLowLatencyModes}" SelectedItem="{Binding SelectedCplLowLatencyMode}" />
                </controls:OptionControl>

                <controls:OptionControl Header="FRL Low Latency Mode" Description="Driver-level low latency control using frame rate limiter logic.">
                    <ComboBox Width="220" ItemsSource="{Binding FrlLowLatencyModes}" SelectedItem="{Binding SelectedFrlLowLatencyMode}" />
                </controls:OptionControl>
            </StackPanel>
        </ui:CardExpander>

        <ui:CardExpander Margin="0,8,0,0">
            <ui:CardExpander.Header>
                <StackPanel>
                    <TextBlock FontSize="14" Text="Hardware and Driver Overrides" />
                    <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Text="Overrides that affect GPU behavior and driver features." />
                </StackPanel>
            </ui:CardExpander.Header>

            <StackPanel>
                <controls:OptionControl Header="Resizable BAR" Description="Allows the CPU to access the full GPU memory range on supported systems.">
                    <ui:ToggleSwitch IsChecked="{Binding EnableRbar}" />
                </controls:OptionControl>

                <controls:OptionControl Header="SILK Smoothness" Description="Controls NVIDIA SILK frame smoothing intensity.">
                    <ComboBox Width="220" ItemsSource="{Binding SilkSmoothnessModes}" SelectedItem="{Binding SelectedSilkSmoothness}" />
                </controls:OptionControl>
            </StackPanel>
        </ui:CardExpander>
    </StackPanel>
</ui:UiPage>
